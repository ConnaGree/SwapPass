// const grids = [...tiles];

// let counter = 0;

// const setGrid = (lettersArr, index) => {
//   lettersArr = shuffleGrid(letters);

//   for (let i = 0; i < grids.length; i++) {
//     grids[i].innerHTML = lettersArr[index];
//     index++;
//   }
// };

// const shuffleGrid = (lettersArr) => {
//   for (let i = 0; i < lettersArr.length; i++) {
//     let j = Math.trunc(Math.random() * lettersArr.length);

//     let temp = lettersArr[i];
//     lettersArr[i] = lettersArr[j];
//     lettersArr[j] = temp;
//   }

//   return lettersArr;
// };

// setGrid(letters, counter);

// console.log(tiles);
